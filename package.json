{
  "name": "wizdao_test_subgraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ wizdao_test_subgraph",
    "create-local": "graph create --node http://localhost:8020/ wizdao_test_subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ wizdao_test_subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 wizdao_test_subgraph"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.26.0",
    "@graphprotocol/graph-ts": "0.24.1"
  }
}
